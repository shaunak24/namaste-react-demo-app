import RestaurantCardList from './RestaurantCardList';
import SearchBar from './SearchBar';

const Body = () => {
  return (
    <>
      <SearchBar />
      <RestaurantCardList />
    </>
  );
};

export default Body;
